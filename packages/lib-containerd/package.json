{
  "name": "@takaro/containerd",
  "version": "0.0.1",
  "description": "gRPC client for containerd",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "start:dev": "tsc --watch --preserveWatchOutput -p ./tsconfig.build.json",
    "build": "./generate.sh && tsc -p ./tsconfig.build.json && cp -r ./proto ./dist/proto",
    "test": "ts-mocha -p ./tsconfig.json src/**/*.test.ts",
    "test:watch": "npm run test -- --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grpc/grpc-js": "^1.6.9",
    "@grpc/proto-loader": "^0.7.0"
  },
  "devDependencies": {
    "@takaro/test": "0.0.1",
    "grpc-tools": "^1.11.2"
  }
}
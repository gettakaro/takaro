{
  "name": "@takaro/socket",
  "version": "0.0.1",
  "description": "An opinionated Socket io client",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "type": "module",
  "scripts": {
    "start:dev": "tsc --watch --preserveWatchOutput -p ./tsconfig.build.json",
    "build": "tsc -p ./tsconfig.build.json",
    "test": "npm run test:unit --if-present && npm run test:integration --if-present",
    "test:unit": "rm -rf ./dist && tsc -p ./tsconfig.json && mocha --config ../../.mocharc.js dist/**/*.unit.test.js",
    "test:integration": "echo 'No tests (yet :))'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@takaro/util": "0.0.1",
    "convict": "^6.2.3"
  },
  "devDependencies": {
    "@takaro/test": "0.0.1"
  }
}
import { FC } from 'react';
import { Container } from './style';

interface ConnectionCardProps {
  name: string;
}

export const ConnectionCard: FC<ConnectionCardProps> = ({ name }) => {
  return <Container>hello</Container>;
};

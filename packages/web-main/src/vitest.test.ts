// tests if vitest works

// Import the testing functions from Vitest
import { describe, it, expect } from 'vitest';

// Describe the test suite
describe('Basic Test Suite', () => {
  // Define a test case
  it('should pass a basic test', () => {
    // Expectation
    expect(true).toBe(true);
  });
});

import { styled } from '../../../styled';

export const FieldSet = styled.fieldset`
  display: block;
  margin: 0;
  padding: 0;
  border: none;
  font-size: ${({ theme }) => theme.fontSize.medium};
`;
